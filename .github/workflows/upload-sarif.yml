name: "UTBot code analysis"

on:
  workflow_dispatch:
    
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
    - name: UTBot code analysis
      uses: slawa4s/Utbot-Action@test-1.0.6
